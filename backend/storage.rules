rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // WARNING: permissive rule — allows anyone to read and write.
      // Use only for testing. Revert to restrictive rules for production.
      allow read, write: if true;
    }
  }
}
